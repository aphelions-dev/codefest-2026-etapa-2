# Solo para `next dev`: el build de produccion recibe estas dos como build args y falla si faltan.
# 127.0.0.1 y no localhost: en Windows localhost prueba IPv6 primero y suma segundos por peticion.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_SURFACE=dashboard
